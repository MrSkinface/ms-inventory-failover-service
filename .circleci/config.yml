version: 2.1
jobs:
  build-and-test:
    docker:
      - image: cimg/openjdk:17.0
    steps:
      - checkout
      - run:
          name: Build and test
          command: mvn clean test

workflows:
  build-and-test:
    jobs:
      - build-and-test